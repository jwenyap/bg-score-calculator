{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "cmd=$(jq -r '.tool_input.command'); echo \"$cmd\" | grep -q '^git commit' && sed -i \"s/const CACHE = 'board-scores-v[^']*'/const CACHE = 'board-scores-v$(date +%Y%m%d-%H%M%S)'/\" /home/user/bg-score-calculator/sw.js && git -C /home/user/bg-score-calculator add sw.js || true"
          }
        ]
      }
    ]
  }
}
